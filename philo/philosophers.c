#include "philosophers.h"

int	main(int argc, char **argv)
{
	t_philo		philo[argc - 1];
	t_my_mutexs	y_mut[argc - 1];

	if (argc < 2)
		return (0);
	printf("OK\n");
	// if (cheack_argv(argc, argv))
	// 	return (error_mes() || 2);
	// if (inicalizacia())
	return (0);
}